import React from "react";
import { PlaceholderFeatureScreen } from "../../components/PlaceholderFeatureScreen";

export function SyncConflictScreen() {
  return (
    <PlaceholderFeatureScreen
      title="Sync Conflicts"
      subtitle="Conflicted offline records are shown here for manager/admin review and correction."
    />
  );
}
